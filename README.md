# 🦆 Duck vs Farmers

Пошаговая игра на графах: уточка пытается добежать до озера и не попасться фермерам, которые используют алгоритм поиска кратчайшего пути.

## 📌 Идея

- Игровое поле — случайный взвешенный граф на 10–13 вершинах.
- Игрок управляет уточкой, фермеры управляются ИИ (A*).
- Цель — добраться до озера или продержаться ограниченное число ходов, избегая поимки.

## 🎮 Правила игры

### Цель

- Добраться уточкой до вершины-озера (обычно последняя вершина графа, `Vn`), **или**
- продержаться заданное количество ходов, не оказавшись на одной вершине с фермером.

### Игровое поле и объекты

- Генерируется связный граф 10–13 вершин с координатами без наложения.
- Рёбра имеют веса:
  - обычные дороги (малый вес);
  - «болота» с большим весом — они усложняют пути для алгоритмов.
- На поле есть:
  - уточка 🦆 (игрок, стартует в `V1`);
  - два фермера 👨‍🌾 (позже может появиться третий);
  - вершина-озеро 🌊 (целевая вершина, обычно `Vn`).

### Ход игры

Игра идёт раундами. В каждом раунде:

1. **Ход уточки**
   - Уточка может перейти только в соседнюю вершину по существующему ребру.
   - Ход совершается кликом по соседней вершине на графе.
   - После хода в лог выводится список всех доступных соседних вершин с пометками:
     - `опасно` — там фермер или рядом с ней стоит фермер;
     - `к озеру ближе` — ход уменьшает расстояние до озера;
     - `болото` — переход по ребру с большим весом;
     - `нейтрально` — нет особых рисков или бонусов.

2. **Ход фермеров**
   - Каждый фермер делает **один шаг** по кратчайшему пути к текущей позиции уточки.
   - Направление рассчитывается алгоритмом A* с учётом весов рёбер.
   - В лог записывается, в какую вершину перешёл каждый фермер.
   - После нескольких ходов (например, после 8-го) может появиться третий фермер в случайной свободной вершине.

### Завершение партии

- **Победа**
  - уточка дошла до вершины-озера;
  - или (если включено правило) выдержала лимит ходов, не будучи пойманной.
- **Поражение**
  - после хода фермеров хотя бы один фермер оказывается в той же вершине, что и уточка.
- **Ничья** (опционально)
  - лимит ходов достигнут, уточка не в озере и не поймана.

## 📜 Лог игры

В правом нижнем текстовом поле отображаются:

- информация о создании нового графа (количество вершин, номер вершины-озера);
- стартовые позиции уточки и фермеров;
- каждый ход уточки и фермеров с указанием вершин;
- строка вида  
  `Возможные ходы: V2 (опасно, к озеру ближе); V3 (нейтрально); V5 (болото, к озеру ближе)`  
  после каждого хода уточки.

Лог позволяет понимать, какие ходы доступны, какие из них рискованные и какие приближают к озеру.

## 🚀 Запуск

После запуска нажмите кнопку **«Новая игра»**, затем кликайте по соседним вершинам вокруг уточки, чтобы двигаться по графу.
